#include <stdio.h>

/* This is a comment. */

int main(int argc, char *argv[])
{
    int distance = 100;
    
    int multiplier = 3;
    
    // this is also a comment
    printf("You are %d miles away.\n", distance);
    
    printf("I bet you are %d times as hungry as me.\n", multiplier);
    
    puts("I don't really care");
    
    puts("Why not?");
    
    puts("Just because");
    
    return 0;
}